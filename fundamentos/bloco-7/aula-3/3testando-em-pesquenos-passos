/* TDD - Desenvolvimento Oriento a teste - Testando em pequenos passos!
O desenvolvimento orientado a testes é um processo cíclico que pode ser descrito em três etapas, Falha / Teste Aprovado / Refatorar código (volta o clico)*/

function myFunction() { // declaração da função myFunction

};

const assert = require('assert');
assert.strictEqual(typeof myFunction, 'function'); // testa se existe a função myFunction declarada
